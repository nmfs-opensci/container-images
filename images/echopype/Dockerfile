FROM openscapes/python:4f340eb

# The default conda env on the openscapes image in notebook
RUN conda install -c conda-forge --name notebook cmocean
RUN conda install -c conda-forge --name notebook echopype
